import React from 'react';
import PropTypes from 'prop-types';

const Test = props => {
    return (
        <div>
            This is test component
        </div>
    );
};

Test.propTypes = {
    
};

export default Test;